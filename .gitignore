# Python
__pycache__/
*.py[cod]
.venv/
venv/
.env

# 运行日志（实盘日志里含盈透账户号）
*.log
server_stdout.log
server_stderr.log
backtest_error.log

# 开发/分析产物
outputs/
.codex-artifact-*/
*.inspect.ndjson

# 系统
.DS_Store
Thumbs.db

# ---------------------------------------------------------------------------
# 策略相关：一律不公开
# ---------------------------------------------------------------------------

# 保存的策略本身
strategy_library/*.json

# 实盘与回测差异分析（含真实持仓权重）
*实盘*

# 回测结果（含持仓权重）
ew9_*_result.json
annual_mdd_verification.json
KMLM2_*_daily_weights.json

# 含策略结构、最终持仓的截图
report_previews/

# 测试报告（含策略名与结构统计）
*.xlsx

# 报告生成脚本（内含策略名与数据口径）
build_test_reports.mjs

# ---------------------------------------------------------------------------
# 注意：data/ 必须提交。
# 它只是公开行情（复权日线缓存），是回测能离线运行的前提，不含任何策略信息。
# ---------------------------------------------------------------------------
