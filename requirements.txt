# 运行回测服务所需依赖。Python 3.10+。
fastapi>=0.110
uvicorn>=0.27
pydantic>=2.0
pandas>=2.0
numpy>=1.26

# 可选：只有在需要从盈透证券 Trader Workstation 增量下载行情时才用到。
# 仓库自带 data/ 缓存，不装也能回测（见 README 的「离线模式」）。
ib_insync>=0.9.86
